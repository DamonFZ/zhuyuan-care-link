/* ============================================================
 * pages/home/index.wxss  —— 首页大盘
 * 色彩策略：复用全局 --primary-color / --primary-light（极简灰蓝）
 * ==========================================================*/

.home-page {
  padding-bottom: 48rpx;
}

/* ---------- 轮播 ---------- */
.swiper-wrap {
  margin: 24rpx 24rpx;
}
.banner {
  width: 100%;
  height: 360rpx;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.06);
}
.banner-img {
  width: 100%;
  height: 100%;
  display: block;
  border-radius: 16rpx;
}
.banner-title {
  position: absolute;
  left: 0; right: 0; bottom: 0;
  padding: 16rpx 28rpx 20rpx;
  background: linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,0,0,0.5) 100%);
  color: #fff;
  font-size: 28rpx;
  font-weight: 600;
  border-bottom-left-radius: 16rpx;
  border-bottom-right-radius: 16rpx;
}
.banner-placeholder {
  width: 100%; height: 100%;
  background: var(--primary-color);
  border-radius: 16rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  color: #fff;
  padding: 0 40rpx;
  box-sizing: border-box;
}
.ph-title { font-size: 40rpx; font-weight: 800; margin-bottom: 12rpx; }
.ph-sub   { font-size: 26rpx; opacity: 0.92; letter-spacing: 1rpx; }

/* ---------- 宫格区 ---------- */
.grid-section {
  margin: 8rpx 24rpx 0;
}
.section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-main);
  padding: 12rpx 4rpx 20rpx;
}
.grid-2 {
  display: flex;
  justify-content: space-between;
  align-items: stretch;
  gap: 24rpx;
}
.grid-card {
  flex: 1;
  background: #ffffff;
  border: 1rpx solid #eef0f3;
  border-radius: 16rpx;
  padding: 32rpx 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  min-height: 240rpx;
  box-sizing: border-box;
}
.grid-card-hover {
  transform: scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.06);
  opacity: 0.95;
}
.grid-icon {
  width: 80rpx;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  font-size: 44rpx;
  border-radius: 16rpx;
  margin-bottom: 20rpx;
}
.grid-icon-primary {
  background: var(--primary-light);
}
.grid-icon-secondary {
  background: #eef0f3;
}
.grid-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-main);
  margin-bottom: 8rpx;
}
.grid-desc {
  font-size: 24rpx;
  color: var(--text-muted);
  line-height: 1.5;
}

.home-tips {
  margin: 40rpx 28rpx 0;
  color: var(--text-muted);
  font-size: 24rpx;
  line-height: 1.7;
}

/* ========== 商户端：全屏大扫码 ========== */
.merchant-wrap {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 48rpx;
  box-sizing: border-box;
}
.merchant-greeting {
  text-align: center;
  margin-bottom: 80rpx;
}
.merchant-hello {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text-main);
}
.merchant-sub {
  margin-top: 12rpx;
  font-size: 26rpx;
  color: var(--text-muted);
}

.scan-btn {
  width: 480rpx;
  height: 480rpx;
  border-radius: 50%;
  background: var(--primary-color);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  box-shadow: 0 12rpx 40rpx rgba(59, 98, 145, 0.28);
  transition: transform 0.15s;
}
.scan-btn-hover {
  transform: scale(0.96);
}
.scan-icon {
  font-size: 140rpx;
  line-height: 1;
  margin-bottom: 16rpx;
}
.scan-text {
  font-size: 44rpx;
  font-weight: 800;
  color: #ffffff;
  letter-spacing: 4rpx;
}
.scan-sub {
  margin-top: 8rpx;
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.9);
}
.merchant-tip {
  margin-top: 64rpx;
  font-size: 26rpx;
  color: var(--text-muted);
  text-align: center;
}
